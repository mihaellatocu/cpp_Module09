#include <iostream>
#include <vector>
#include <deque>
#include <cstdint>

class PmergeMe
{
private:
    std::vector<int> vector;
    std::deque<int> deque;
public:
    PmergeMe(/* args */);
    ~PmergeMe();
};

